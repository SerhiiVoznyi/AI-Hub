# AI-Hub Context

AI-Hub is a repository of reusable AI artifacts organized by competency rather than by vendor or model.

Priorities:

- keep artifacts small, composable, and easy to cross-link
- prefer tool-agnostic guidance where possible
- mark tool-specific dependencies explicitly when they exist
- avoid adding new top-level categories until content volume justifies them
